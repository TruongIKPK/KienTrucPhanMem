services:
  nodejs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nodejs_app
    environment:
      NODE_ENV: development
      PORT: 3000
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
