services:
  react-nginx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: react_nginx_app
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
