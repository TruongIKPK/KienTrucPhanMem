services:
  flask:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flask_app
    environment:
      FLASK_ENV: development
      FLASK_APP: app.py
      PORT: 5000
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    restart: unless-stopped
