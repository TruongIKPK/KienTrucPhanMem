# Khi muốn chạy tất cả PU trên cùng 1 máy (test cá nhân, không cần LAN)
# Copy file này thành .env.local — Vite sẽ ưu tiên .env.local đè lên .env

VITE_PU1_URL=http://localhost:8081
VITE_PU2_URL=http://localhost:8082
VITE_PU3_URL=http://localhost:8083
VITE_PU4_URL=http://localhost:8084
# Bỏ comment nếu muốn mọi người dùng chung một userId (ví dụ u1) thay vì UUID/localStorage.
# VITE_USER_ID=u1
